import { EducativaActividades } from './educativa-actividades';

describe('EducativaActividades', () => {
  it('should create an instance', () => {
    expect(new EducativaActividades()).toBeTruthy();
  });
});
